[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"

[packages]
pyApp = "~=4.0"
{% if cookiecutter.use_sqlalchemy == "y" %}pyApp-SQLAlchemy = "*"{% endif %}
{% if cookiecutter.use_smtp == "y" %}pyApp-SMTP = "*"{% endif %}

[requires]
python_version = ">=3.6"
